# Continuing still.

- Start with `docs/PROJECT-STATUS.md` for the current branch, verified checkpoint, next increment and outstanding release gates. Read the relevant dated review for evidence; older reviews are historical.
- Rikesh is learning: explain the core concept, make small demonstrable increments, provide an exercise, and record what was actually verified.
- Use `graphify-out/graph.json` to navigate relationships when available. Graphify output is a generated navigation aid; verify status and behavior against current source and tests. Refresh it after meaningful architecture/documentation changes, excluding local tools, dependencies, generated native output and private data.
- Read `mobile/AGENTS.md` before modifying the Expo app. Use `.cmd` npm/npx launchers in Windows PowerShell.
- Follow Lesson 2 for this laptop's short-path Android build setup. Reuse toolchains/caches and the physical S20; avoid routine clean builds or installing an emulator.
- Never put real journals, media, keys, local diagnostics or APKs in the public repository. Do not claim production readiness from a debug build or limited encryption checks.
- At a milestone or wrap-up, update `docs/PROJECT-STATUS.md` with completed work, precise validation, remaining work and reproducible resume instructions.
