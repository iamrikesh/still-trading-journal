# still. — resume here

## Current checkpoint (2026-09-24) — journal feature milestone complete

Branch **codex/android-device-setup**. Reviewed implementation checkpoint **c68e85b**; this documentation handoff follows it on the same branch. All four approved journal deliverables are implemented, task-reviewed and covered by bounded S20 acceptance. The whole-milestone review and its single correction wave are approved with no open findings in scope. Nothing is merged into main or released. Use the Git commands below to verify the actual local/remote documentation tip when resuming.

- **Completed:** manual sessions/grouping, recoverable original notes and separately dated reflections; editable reminder buttons with text/images/imported audio; Older/Newest history and persisted System/Light/Dark. Captured moments retain their original words when a current card changes. Global history displays current session/archive metadata separately.
- **Source validation:** final full host suite **166/166**, TypeScript pass and Android export **691 modules**. Independent final scoped review ran56/56 plus stale-owner/paging/audio probes. Native **79/79** across9 suites remains the latest native result; native source did not change during UI/final corrections. Incremental arm64 APK, SQLCipher packaging, v2 signature and permission checks passed before install with `-r`. No clean build, new dependencies, emulator or data reset.
- **Sessions/writing evidence:** Start/Resume/End across restart, recoverable drafts, finalisation after End, moment/session reflections and follow-ups, regrouping with original capture preserved, adjusted cross-midnight boundaries and Archive/Restore. Safe active Record→End saved14117ms of synthetic audio; only that sample was deleted. See [sessions evidence](reviews/2026-09-24-sessions-acceptance.md).
- **Reminder evidence:** real picker Cancel/import, tiny PNG/WAV Save/reopen/display, explicit Play/Stop/background cleanup; oversized image/invalid WAV/Opus rejection preserving previous media; AAC acceptance/playback;4MP PNG preview and exact4MiB WAV playback. Draft Cancel, Stay/Save/Discard, theme change and ordinary Home/Recents draft retention passed. Move/Archive/Restore, image removal/restoration and immutable old capture text passed. See [reminder evidence](reviews/2026-09-24-reminders-history.md).
- **History/appearance evidence:** actual UI Older crossed50 rows with51 equal-time synthetic moments. Entry00 opened writing and grouping/Record controls; its note was finalised. Only rows01..50 were deleted after exact ownership/content checks. Dark, Light and System survived process restarts. On c68e85b, populated→empty session switching showed only the selected owner's rows; history added then removed Archived during the owned session's Archive/Restore check. Delayed/failing races remain host-tested.
- **Final preserved state:**24 moments,7 active/0 archived cards, System,192225 reminder attachment bytes. Original17 media files/3920423 clip bytes and all11 earlier metadata pairs matched. Sep23/Sep24 sessions and writing remain, including adjusted/original boundaries and the latest finalised follow-up. No active session, zero pending, both audio owners idle, no reminder temporary.
- **Retained exercises:** **Feature test reminder Sep24** has the64x64/181-byte PNG and12000ms/192044-byte WAV; its updated current support differs from its earlier captured moment. Ended **Feature test history Sep24** contains only entry00 with its final note and is restored, not archived. Earlier Sep23/Sep24 examples remain.
- **Environment:** app left on Now, audio idle. Fast Refresh verified true, stay-awake0 and timeout600000 unchanged. Exact eight owned picker fixtures and empty phone folder removed; owned UI dump removed. Metro, USB8081, installed native APK, S: toolchains/caches and ignored local helpers/review handoff retained. No APK/private media/local diagnostics are published. If Metro or USB forwarding has stopped, restore them using Lesson2; allow the development bundle to finish loading before inspector checks.
- **Resume:** read this checkpoint and dated reviews; inspect `git status --short --branch`, `git log -5 --oneline` and the matching remote tip. Reuse the installed APK and cold-launch reviewed JS after source changes. No native rebuild is needed for these final TypeScript corrections. Preserve existing data and synthetic exercises. Before another feature or security increment, choose one remaining roadmap item and define a small demonstrable scope; do not repeat this completed milestone.
- **Remaining release gates:** app unlock, tested encrypted backup/restore, missing-key recovery, signing/privacy and release validation. Physical low-space, call/headphone audio interruptions, earlier recording resource investigations and broader device/accessibility checks also remain. Host-only failure probes and short memory snapshots do not establish universal recovery, peak-memory limits or leak freedom.
- **Exercise:** compare the current **Feature test reminder Sep24** support with its earlier My moments capture. Explain why editing the card changed one but not the other. Then open the final note in **Feature test history Sep24**. See [Lesson8](learning/08-personal-reminders-and-history.md).

## Previous checkpoint (2026-09-23) — requested stop

Rikesh asked to stop at the nearest logical checkpoint after the interruption. Work remains on **item 2**, but do not start another increment during this wrap-up. Branch: **codex/android-device-setup**. Reviewed source checkpoint: **2be987d**, following session/writing storage and UI implementation plus review fixes. This documentation commit closes the handoff; nothing is merged into main.

- **Implemented:** schema3 sessions, explicit moment grouping, recoverable notes/reflections, immutable finished writing, adjusted boundaries and Archive/Restore; screens and controllers are connected. End cancels pending Record requests and waits for confirmed audio release. Review fixes protect dirty drafts, latest-owner lookups, Done retry, background flush and editor locks during finalise/discard.
- **Verified:** final **123/123 host tests**, TypeScript and Android Metro export (685 modules). Storage and UI task reviews are approved after their scoped fixes. S20 storage/API checks and a bounded UI recovered-draft edit/Done/read-only/cold-restart check passed. See the [dated evidence and limitations](reviews/2026-09-23-sessions-writing.md); final UI locking fixes were host-verified after the device was stopped.
- **Preserved:** exact original 17-file / 3920423-byte clip set, earlier eleven sample metadata pairs, native idle and zero pending. Retained the clearly synthetic **Feature test Sep23** ended/restored session with two moments and writing for Lesson7. The moment labelled Feature test draft now contains the finalised text “Feature test edited on S20.” No microphone capture, key replacement, data reset or native rebuild in this increment.
- **Stopped:** app process and Metro port 8081 listener absent; USB 8081 forwarding removed. Restored phone stay-awake 0; screen timeout remains 60000 ms. Owned phone fixture files and UI dumps removed. Existing app/APK, caches, S: alias and ignored laptop helpers retained.
- **Next:** finish [sessions/writing plan](superpowers/plans/2026-09-23-sessions-writing.md) Task3 device acceptance: Start/Resume/End across restart, reflection follow-ups, regrouping, boundary correction, Archive/Restore, retry/discard and safe recording interaction when suitable. Do not repeat implemented Tasks1/2. Then execute the separate [reminders/history plan](superpowers/plans/2026-09-23-reminders-history.md). Custom emotion/reminder media, general older-history paging and persisted theme are planned, **not implemented**.
- **Resume:** read this file, [milestone tracker](JOURNAL-FEATURES.md), dated review and plan; check git status/log. Follow [Lesson2](learning/02-android-device-build.md) to restore environment, connect/unlock S20, forward USB 8081 and start Metro with IPv4 preference from S:\mobile. Cold-launch the installed development app; no rebuild is required merely to resume. Open Sessions → Feature test Sep23. Recheck current device data before testing; preserve additions since this baseline. Plan ledgers and review packages remain under ignored .superpowers/sdd for local continuity; public docs carry the durable resume state.
- **Learning:** [Lesson7](learning/07-sessions-and-writing.md) explains why moment capture, session ending and note finalisation have separate times. Predict which times remain unchanged when moving a moment.
- **Still separate:** previous recording resource/interruption investigations and release gates (app unlock, encrypted backup/restore, missing-key recovery UX, release signing/privacy/permissions). This checkpoint does not complete all journal features or establish production readiness.

## Previous recording baseline (2026-09-23)

Continued from published `a817b41` on `codex/android-device-setup`; fixed the reproduced missing denial explanation. See the [denial-feedback fix and device evidence](reviews/2026-09-23-permission-start-cancellation.md#denial-feedback-fix). Native/runtime source and dependencies are unchanged.

- **Fix:** denial feedback survives background/foreground for the same selection. Any new selection invalidates it, including A → B → A. Granted results still pass the existing recording cancellation checks; showing a message cannot restart capture.
- **Verified:** two denial regressions failed on old code; final **87/87 tests**, TypeScript and independent review passed. Actual Android Deny now shows the explanation with audio idle and zero pending. Home during the permission prompt and return also stayed stopped. Granting access stayed stopped, and a fresh Record/Stop saved a synthetic sample. Native playback and its temporary file were observed on replay, then the sample was deleted.
- **Device limit:** the cycle helper's first Play attempt did not observe a playback temporary and aborted. Follow-up found the clip saved, no pending and native stopped; replay worked. This is not an uninterrupted full-cycle pass, and the cause of the brief first playback stop was not established. See the review rather than attributing it to the denial fix.
- **Preserved/final:** the starting baseline included additional saved audio: **17 files / 3920423 bytes**. Final cold restart restored that exact file set and usage, native audio idle, zero pending and no cycle-test clips; the original eleven sample duration/byte pairs also matched. Only the new synthetic cycle clip was deleted. Microphone grant and prior flags were restored; sleep settings and app data were not reset. Owned UI dumps removed; app stopped on Now.
- **Resume:** check branch/status and Metro/USB forwarding, then explicitly locate **Cycle test Sep23** in My moments; it is no longer the newest entry. Verify its heading and empty state before creating disposable samples. Reuse the installed APK, S: alias and toolchains. Preserve all existing audio, including additions since older checkpoints.
- **Next small increment:** real call/headphone interruptions when available; correlate audio-focus evidence if the brief first-Play stop recurs. Original cycle17 failure, other navigation variants, low-space/save-fault recovery, memory limits and release gates remain open. No native rebuild was required for this fix.
- **Exercise:** why should backgrounding cancel a recording request but preserve its denial explanation, while A → B → A invalidates that explanation? See Lesson6.

## Previous permission-start fix checkpoint (2026-09-23)

Continued from published `b1a9c79` on `codex/android-device-setup`. Fixed a reproduced delayed recording start after leaving and returning during the permission request. See the [permission-start cancellation review](reviews/2026-09-23-permission-start-cancellation.md). The memory/recovery checkpoint below is historical; its cycle17 failure is still not attributed to this defect.

- **Behavior:** backgrounding now invalidates a pending Record request, even if permission resolves after returning. The runtime checks an existing microphone grant before opening a permission request. If a permission prompt backgrounds the app, granting access leaves it stopped; press Record again to capture.
- **Validation:** two new regressions failed on old code; final full suite **83/83**, TypeScript and independent review passed. On S20, the old implementation twice started on return without another Record press. Final code passed normal Record/Stop/Play/Delete, stopped a rapid Record/Home capture without restarting on return, and stayed idle after an actual **While using the app** grant. A fresh Record after granting completed another full cycle. Read the review for timing and intermediate-build limits.
- **Preserved/final:** cold restart confirmed native audio idle, zero pending, zero cycle-test clips and 3896667-byte usage. All eleven retained samples keep their original durations/byte counts. Vault: 15 files, no plaintext temporaries or pending ciphertext. Only synthetic probe samples/intents were deleted. Microphone grant and prior decision flags were restored; sleep settings and app data were not reset. Owned UI dumps were removed; app is on Now, stopped.
- **Environment:** reused installed debug APK, S20, Metro, S: alias and toolchains. No native/dependency changes or rebuild. Native checks remain the September22 evidence. Local probes/diagnostics stay ignored; no private media or keys are published.
- **Next small increment:** actual Deny-button/navigation checks, then call/headphone interruptions when available. Original cycle17 failed-start diagnosis, device low-space/save faults, memory limits and existing release gates remain open. Do not infer a twenty-cycle pass or production readiness.
- **Resume:** check `git status --short --branch` and `git log -5 --oneline`; check Metro `/status` and USB forwarding before restarting services. Connect/unlock the S20 and open **My moments → Cycle test Sep23 → Open voice clips** for disposable samples. Cold-restart after changing runtime callbacks: Fast Refresh can retain the controller created by `useState`. Preserve the four earlier plus seven September23 samples; no data reset or APK rebuild is needed.
- **Exercise:** why can a current foreground check allow an old Record request after returning? Explain why cancelling the request when leaving is also necessary. See Lesson6.

## Previous memory and recovery checkpoint (2026-09-23)

Continued from published `3a38aa7` on `codex/android-device-setup` after Rikesh unlocked the S20. See the [longer memory-baseline review and recovery follow-up](reviews/2026-09-23-recording-memory-baseline.md). App/native source is unchanged. This checkpoint supersedes the resource-run handoff below.

- **Measured:** sixteen complete Record/Stop/Play/Delete cycles in one app process over a run lasting 595.8 seconds. Each completed cycle restored the original 15-file vault set. No JavaScript inspection or forced garbage collection during measurement. Native allocated memory at the 30-second idle checkpoints after cycles 5/10/15 was 115244/113417/114609 KiB (110.8–112.5 MiB). Allocation fell substantially during cycles 8 and 13; descriptors stayed 160–162 and threads 47–51. Total PSS remained above startup. This is bounded evidence, not proof of leak freedom.
- **Interrupted:** the twenty-cycle target was not completed. Cycle17 displayed “Recording could not start”; the helper aborted when Stop was absent and sent Home. Its overly broad Recording-prefix assertion was tightened to the timer pattern. That local-helper fix does not explain the app failure. An earlier, separate attempt stopped after UIAutomator exit137; its generated sample was saved and explicitly deleted before restarting the measured run.
- **Recovery verified:** after unlock, the synthetic cycle moment had one pending operation and Record was disabled. Retry save retained it with an error; there was no remaining audio staging file. Confirmed Discard removed only that failed synthetic intent and enabled Record. Five subsequent Record/Stop/Play/Delete cycles passed in the existing process, each restoring the exact original vault file set. This is a separate segment; the failed start did not recur and its cause remains unresolved.
- **Final device verification:** cold restart restored the inspector connection and confirmed native audio idle, zero pending operations, zero cycle-test clips and usage of 3896667 bytes. All eleven retained lifecycle samples have their original durations/byte counts. Vault inspection found 15 files, no staging/playback plaintext and no pending ciphertext. The empty **Cycle test Sep23** panel was reopened; owned temporary UI dumps were removed. No data was cleared. Earlier microphone/free-space snapshots do not identify the cause at failure time.
- **Environment:** Metro `/status` returned running and USB port 8081 forwarding is present. Reuse the installed APK, S: alias, toolchains and caches via Lesson2; no new build is needed for inspection. This increment changed no app source, permission/sleep settings or dependencies and cleared no app data. Local helper/diagnostic files remain ignored. Host/native tests were not rerun; prior evidence is dated below.
- **Next small increment:** investigate the failed-start boundary with correlated foreground/focus/power/admission evidence before any behavioral fix; a generic failure message and five successful retries do not establish its cause. Coordinate call/headphone interruption when available. Actual Deny-button/navigation behavior, device low-space/save-fault cases and existing release gates remain open.
- **Resume:** read the review's recovery follow-up; check `git status --short --branch` and `git log -5 --oneline`. Connect/unlock the S20, check Metro `/status` and USB forwarding, then open **My moments → Cycle test Sep23 → Open voice clips**. Confirm it is empty with no pending operations before disposable samples. Reuse the installed APK; preserve the four earlier plus seven September23 samples. Do not combine separate measurement segments into a continuous-run claim.
- **Exercise:** why can a pending operation exist without playable audio, and why can Retry not create that missing audio? Trace the durable intent created before native capture in `journalSession.ts`. See Lesson6 and the dated review.

## Previous resource checkpoint (2026-09-23)

Continued `codex/android-device-setup` from published `cb0f426` for bounded resource checks. This increment adds device evidence and documentation; app/native source is unchanged. See [September 23 lifecycle checks](reviews/2026-09-23-recording-device-checks.md) and the [resource-cycle review](reviews/2026-09-23-recording-resource-cycles.md). Earlier sections below are historical.

- **Latest device verification:** ten Record/Stop/Play/Delete cycles restored the exact original vault file set, 3896667-byte usage and zero pending operations after every cycle. A five-cycle comparison without JavaScript inspection also restored the file set; its earlier interrupted attempt is separately documented. Final cold restart showed native audio idle, zero cycle-test clips and zero pending operations. Memory remains an open investigation: debugger-only controls raised native-heap PSS, and the comparison still rose without debugger evaluation. No leak-free or long-run memory claim.
- **Earlier same-day validation:** 81/81 host tests, TypeScript and whitespace checks passed before the resource increment; unchanged app source was not retested on the host for this documentation-only follow-up. On the S20, Android-enforced denial created no clip; two immediate-Stop clips saved at 488/534 ms; deliberate screen-off stopped/saved a 3576 ms clip and unlocking did not resume it. Native tests/build remain September22 evidence. No production-readiness claim.
- **Preserved data:** the earlier four clips and the seven **Voice test Sep23** clips retain their exact baseline durations/byte counts after final restart. **Cycle test Sep23** remains as an empty synthetic moment; only clips created for these cycles were deleted. No audio, keys or journal prose was exported. No vault plaintext temporaries remained. Final usage: 3896667 bytes; laptop free disk: 10.64 GiB (whole-drive reading, not a test-storage delta).
- **Environment:** existing installed APK, toolchains and caches reused; S: alias and USB port 8081 forwarding remain available. Metro is running with IPv4 preference and two workers; the empty cycle-test panel is open. Microphone access is granted as restored earlier; this increment changed no permission/sleep settings and cleared no app data. Owned temporary UI dumps were removed. Development services may need restarting in a later session.
- **Next small increment:** establish a longer, controlled memory baseline without Runtime.evaluate during the measured interval; the short runs do not establish a plateau or a root cause for retained memory. Use the review's reproduction steps and distinguish debugger overhead from app allocations before proposing fixes. Calls/headphones were unavailable; coordinate those checks when Rikesh has either. Deliberate Deny-button interaction, permission navigation, device low-space/save-fault recovery and startup edges remain unrun.
- **Resume:** read this section and both September23 reviews; run `git status --short --branch` and `git log -5 --oneline`. Connect/unlock the S20. Check Metro `/status` and USB forwarding before starting another server; use [Lesson2](learning/02-android-device-build.md) if either stopped. Use **My moments → Cycle test Sep23 → Open voice clips** for fresh disposable samples, or Voice test Sep23 to inspect retained lifecycle examples. No rebuild is required. Never clear data to repeat a test.
- **Exercise:** if deleting a sample restores the exact original files but memory stays higher, what did the test prove and what remains uncertain? Why does a debugger-only control matter? See [Lesson6](learning/06-recording-lifecycle.md).
- **Release gates remain:** app unlock, encrypted backup/export/restore, missing-key recovery UX, release signing/privacy and release permission/backup checks. Slow development cold launches still need investigation; Android activity-launch times are not end-to-end usable-app benchmarks.

## Historical September 22 handoff

Updated at session wrap-up, 2026-09-22. Record / Stop / Play is committed and pushed as **`bc32954`** on **`codex/android-device-setup`**; GitHub's branch SHA was verified as `bc3295459fe61c2d7bb205dc1795f00508873854`. This final handoff/Graphify commit follows it on the same branch. Nothing is merged into `main`. See [today's recording evidence](reviews/2026-09-22-record-stop-play.md) and [Lesson 6](learning/06-recording-lifecycle.md). Earlier checkpoints below are historical.

### Session wrap-up and next session

- **Stopped and verified:** Metro's port 8081 listener is absent, still. has no running phone process, USB port 8081 forwarding is removed, and no Java build process remains. Original phone sleep setting is restored. Existing journals, keys, three voice samples, the installed app, one local APK and reusable toolchains/caches are retained.
- **Fresh wrap-up validation:** 81/81 host tests and TypeScript passed; native code remains the same 62-test/build-verified source. The dated review separates real S20 outcomes from host tests and unrun release gates. No private audio, journals, keys, diagnostics, APKs or dependencies are included in the push.
- **Next small increment:** verify actual microphone denial, immediate Stop/rapid taps and deliberate screen-lock stop-and-preserve on the S20, then document the outcomes and fix only reproduced issues. Resume with sample phrases and keep private conversation out of earshot; opening the app itself must never start capture. Do not clear app data or overwrite the retained samples to repeat a test.
- **After that:** call/headphone interruption, repeated-cycle memory/resource measurements, device low-space/save-fault recovery; investigate slow development cold launches. Release gates remain app unlock, encrypted backup/export/restore, missing-key recovery UX, release signing/privacy and release permission/backup checks. MVP work beyond this recording increment remains in the project plan.
- **Resume:** read this file and the September22 review; run `git status --short` and `git log -5 --oneline`. Follow [Lesson2](learning/02-android-device-build.md) to restore the S: alias if needed and the existing JDK/SDK/Gradle environment. Connect/unlock/authorize the S20. From `S:\mobile`, run `adb -d reverse tcp:8081 tcp:8081`, set process `NODE_OPTIONS=--dns-result-order=ipv4first`, and run `npx.cmd expo start --dev-client --localhost --max-workers 2`. Open still. at `http://127.0.0.1:8081`; check Metro `/status` if needed. The installed APK is reusable; no native rebuild is needed merely to resume.
- **Learning checkpoint:** open **My moments → Voice test Sep22 → Open voice clips**. The retained clips are 0:47, 0:09 and 3:59; all survived restart. Predict what temporary storage does during Play/Stop using Lesson6. Use the refreshed Graphify map for navigation, then verify claims against source and the dated review.

### Recording increment checkpoint (2026-09-22)

- Implemented several clips per saved moment with a four-minute ceiling, deterministic private staging and encrypted saving. Lesson 6 explains the lifecycle and prediction exercise.
- Expo Audio 57.0.5 supplies permission requests; the approved native-adapter fallback owns Android capture/playback to avoid automatic resume and register staging before capture. Native limits are 239500 ms / 4 MiB minus finalization allowance, with strict 240000 ms / 4 MiB final validation.
- Saved-moment controls and history clip detail include timer, Record/Stop/Play, retry/discard/delete, 20-clip pages, usage warning and bounded pending-owner navigation outside the recent-50 history. Unconfirmed resource release keeps an app-wide Stop again control and blocks new audio.
- Verified **81/81 Node tests**, TypeScript, **62 native JUnit tests**, whitespace checks and Android Metro compilation. Independent reviews resolved dropped Stop, unreachable deletion recovery and native/UI release-failure ownership findings with red/green regressions.
- Incremental ARM64 APK build passed in 5m51s (34 executed / 335 up-to-date); SQLCipher packaging and v2 signature passed. Microphone permission is present, backup false, no audio foreground services. Generated debug overlay permission remains; release permissions/backup remain open gates.
- Installed the single current APK with install -r, preserving app data. Actual capture, Stop, Play, app-switch stop/save, alarm/notification interruption and cold-restart persistence were observed on the authorized S20. The uninterrupted limit test automatically saved **239.527 s / 1965396 encoded bytes**, below four minutes. Rikesh reported earlier playback clear enough. Two short clips plus the full-length clip are retained; two extra samples were deleted through UI confirmation. Total vault usage after cleanup: **2474347 bytes / 2.36 MiB**, zero pending saves. See the dated review for exact scope.
- Reused checkout/toolchains, S: alias and caches; no clean build, emulator or APK copies. Latest disk: **13.71 GiB free**, initial 13.75 GiB. During testing Metro used IPv4 port 8081 and two workers; it is now stopped as described above. The scoped Kotlin workaround applies at task-graph-ready time; see Lesson3.
- Device-driven fixes: load Expo Audio with the main bundle instead of delaying Record on a lazy development chunk; synchronize current AppState when effects attach after Fast Refresh; hide pending-failure labels for normal active capture. No extra native build was needed. Development cold launches were unusually slow and remain a separate environment issue.
- Final cold restart confirmed the same three saved clips, unchanged duration/size/usage, **zero pending** and native audio **idle**. Deleted samples and plaintext temporaries remained absent. Original phone sleep setting restored and temporary UI diagnostics removed. Final source checks after device fixes: **81 tests and TypeScript passed**; native source is unchanged from the 62-test build. The increment is pushed; services were stopped at wrap-up.
- Resume: keep the connected S20 unlocked and still. foreground. Open **My moments → Voice test Sep22 → Open voice clips** for the three samples; follow Lesson6's prediction exercise. Continue only the unverified checks listed in the dated review: actual denial, immediate/rapid taps, deliberate screen lock, calls/headphone changes, low space and resource profiling. Existing journals/keys are retained. Release/backup/app-unlock gates still apply.

## Previous checkpoint (2026-09-21)

The first development increment runs on the Samsung S20 (SM-G981U1, Android 13). Emotion tap → immediate injecting logic → timestamped saved moment → history → deletion works. The native clip vault is connected to a single SQLCipher journal session with schema-2 migration and startup recovery. On S20, two generated clips survived an interrupted save and cold restart; their coordinated deletion remained complete after another cold restart. See the [September21 integration evidence](reviews/2026-09-21-native-clip-integration.md). The September22 implementation and actual microphone checks supersede this checkpoint above. This is a development APK requiring Metro, not a standalone release or the full MVP. Use sample moments until the release gates are met.

Repository: https://github.com/iamrikesh/still-trading-journal

Current work is on `codex/android-device-setup`. At Rikesh's wrap-up request, all completed clip-recovery work through `f9e1587` was pushed to GitHub. `git ls-remote` verified `f9e158702a4e31a1c5159e1004257dea82633a93` on the feature branch. This handoff/Graphify update follows that checkpoint on the same branch; verify the current tip with the commands below. Earlier published checkpoints were `45862b2` (native file proof) and `5ab68aa` (journal device verification). Nothing has been merged into `main`. Check `git status` and `git log -5 --oneline` first.

## Completed and verified

- React Native, Expo SDK 57 and TypeScript foundation; six starter emotion/urge cards including Unsure.
- Original text injecting logic appears independently of asynchronous saving. Each tap captures its original timestamp and reminder text.
- Visible saving/failure/retry states. Retry preserves the moment ID and time. Unsaved drafts and displayed history are bounded to 50; older saved rows are not deleted by the display limit.
- Native SQLCipher database with a random key protected by SecureStore; no plaintext native fallback. Web and Expo Go are explicitly temporary, in-memory demos.
- Recent history, confirmed deletion, system/light/dark appearance. Theme selection is not yet persisted.
- 67 Node tests and TypeScript checking passed, including host coordinator and session/exercise recovery cases. The native media module passed 47 real-Tink JUnit tests. Earlier browser interaction checks and web/Android bundle exports belong to earlier checkpoints; see the dated review records for exact scope.
- S20: fresh FOMO tap saved, appeared in history, survived cold restart, and stayed deleted after deletion plus another restart. The pre-existing moment was preserved.
- Native SQLCipher reported `4.7.0 community`; protected-key presence and rejection of an unkeyed database read were verified. This is bounded evidence, not a complete security audit.
- APK signature and the new `npm.cmd run verify:apk` packaging check passed.
- Native synthetic media proof: 65,536-byte fixture prepared, full app process stopped/relaunched, then all ten native checks passed on S20. Only ciphertext and encrypted keyset remained in the dedicated no-backup proof directory. No journal keys/rows were read or changed by the proof.
- Portable Java, existing Android SDK and one ARM64 build workflow are set up. No new emulator or Android Studio was installed. Latest free disk: 15.02 GiB, versus 14.86 GiB before native clip/session integration; an intermediate reading was 14.80 GiB. These are whole-drive observations, not a precise app storage delta.

## Resume development on this laptop

1. Open this repository in Codex and read this file, the latest Android review and Lesson 2.
2. Check the branch/worktree state. Connect and unlock the S20; authorize USB debugging if prompted.
3. Follow [Lesson 2](learning/02-android-device-build.md) for terminal environment setup. Use the `S:` alias and short Gradle cache path consistently for native builds. `S:` points to the existing project; it is not another copy. Recreate it after reboot if absent, never overwrite another drive mapping.
4. Start Metro with IPv4 preference and two workers, then forward USB port 8081. Open the installed development app at `http://127.0.0.1:8081`.
5. For JavaScript-only edits, use Metro. Rebuild the APK only after native dependencies/configuration change; run `verify:apk` before installing.

**Historical September21 wrap-up:** Metro was stopped, the app force-stopped and USB forwarding removed. The September22 session restarted them; use the current checkpoint above. Opening this development APK alone does not start a standalone app. Installed tools, useful caches, the current APK, phone data and media keyset are retained. The S: drive alias may remain until reboot; it costs no duplicate storage. Native builds used single-use Gradle daemons that exit after completion.

### Setup pitfalls already resolved

- PowerShell blocks npm/Codex `.ps1` launchers: use `npm.cmd`, `npx.cmd` and `codex.cmd`; do not change execution policy merely to run these.
- Gradle rejects a trailing period in a project name: Expo's native name is `still`; UI branding remains `still.`.
- Long Windows paths caused Ninja failure and missing SQLCipher runtime-library metadata. The `S:` alias plus `S:\.local-tools\gradle` fixes this. No OpenSSL dependency patch is part of the final solution.
- Metro bound only IPv6 until `NODE_OPTIONS` included `--dns-result-order=ipv4first`. Use the IPv4 URL for USB forwarding.
- Expo SDK 57 prebuild recreates native folders by default. Use `--no-clean` for compatible repeat configuration changes to retain build files; do not routinely run Gradle `clean`.
- The local Gradle init file makes SQLite use the project's NDK, avoiding a second NDK download. It is documented in Lesson 2 and ignored by Git.
- If Codex says a thread has an active writer, do not resume that same thread concurrently. Start a new terminal session in this repository and use this handoff.

## Next learning increment

The tap-to-save source walkthrough was covered on 2026-09-21: `mobile/App.tsx` opens support without awaiting storage; `mobile/src/journal/controller.ts` captures ID/time/reminder text and publishes saving state before persistence; `mobile/src/storage/sqlJournal.ts` inserts once per ID and queries the newest 50. Retry retains the original snapshot. Failed drafts live only in memory; successful write and history-read status are separate.

Exercise completed (user-reported S20 evidence, 2026-09-21): Rikesh confirmed a fresh FOMO tap showed saved status and appeared in My moments. After the temporary Metro edit, he confirmed the new entry contained "I can pause. There will be another setup." while the older entry retained "A moving price is not an instruction. I can let this move go." This demonstrates reminder snapshots across taps. The starter source sentence has now been restored to its original text; saved phone entries were not edited or deleted. No new restart/encryption checks were performed in this exercise.

The temporary reminder edit passed `npm.cmd run typecheck` and `git diff --check`; Metro's health endpoint returned `packager-status:running`. Phone display and history comparison were subsequently confirmed by Rikesh as described above. No build or new dependencies were needed.

The voice design is approved: **several clips per moment, four minutes per clip**, foreground stop-and-preserve, and private temporary plaintext during capture/playback with encrypted committed files. The first native-file proof is complete; see [Lesson 3](learning/03-encrypted-files.md) and the [2026-09-21 evidence](reviews/2026-09-21-media-vault-proof.md).

The metadata/file coordinator and native session integration are implemented: see [Lesson 4](learning/04-recoverable-clip-saving.md), [Lesson 5](learning/05-native-clip-recovery.md) and the [latest integration evidence](reviews/2026-09-21-native-clip-integration.md). The installed opener now migrates to schema 2, owns all operations through one queue, and recovers durable intents on startup. Earlier host-only reports describe their historical schema-1 phone state.

Record / Stop / Play is now implemented; follow [Lesson6](learning/06-recording-lifecycle.md) and the [September22 review](reviews/2026-09-22-record-stop-play.md) for actual microphone evidence and remaining checks. The durable clip namespace is separate from the earlier debug proof; do not route recordings into the synthetic proof namespace.

The dated checkpoints below preserve the sequence of work. Their earlier schema, push and server states are historical; use the current checkpoint and session wrap-up for resuming.

### 2026-09-21 planning checkpoint

- Started on clean `codex/android-device-setup` at `8ffa557`. Fresh `npm.cmd test` passed all 20 tests; `npm.cmd run typecheck` passed. These are host checks, not new phone/security evidence.
- C: free space was 15.02 GiB using an authorized Windows disk query. The sandbox's zero reading was invalid. No dependencies, native builds, APK copies, emulator, Metro server or device-data operations were needed.
- Rikesh chose **several clips per moment**. Propose starting capture only after the moment is saved, with a separate immutable recording ID, moment ID and recording timestamp for each clip. Adding a clip appends rather than overwrites. Show a bounded/paged clip list with per-clip Play, Delete and save/retry status; one active capture at a time. Recording must capture its owner ID once and never follow a later selected emotion. Session grouping remains deferred.
- Proposed small sequence: prove encrypted file commit/recovery with a tiny synthetic fixture; add foreground Record / Stop / Play; verify interruption and failure handling on the S20. Support text stays independent of microphone permission and media saving.
- SDK 57 [Audio docs](https://docs.expo.dev/versions/v57.0.0/sdk/audio/) confirm recording/player APIs, runtime microphone permission, and cache as the default recording location. Plan explicit private durable staging with backup exclusions, and disable both background recording and playback. `expo-audio` is not installed; `mobile/app.json` currently blocks `RECORD_AUDIO`. Installing/configuring audio will require a native rebuild using Lesson 2.
- Compare native file streaming encryption (recommended to evaluate) with buffered encryption using existing `expo-crypto`, or a custom capture-to-encrypted-stream pipeline. The latter adds substantial recorder/codec work. SDK 57 [Crypto docs](https://docs.expo.dev/versions/v57.0.0/sdk/crypto/) and installed source expose AES-GCM buffers, not a ready file-streaming interface. [Tink Streaming AEAD](https://developers.google.com/tink/streaming-aead) is a candidate to evaluate behind an Android adapter, not a selected or integrated dependency.
- Proposed save contract: persist a recording intent, stop/finalize and validate the private staging file, encrypt to a temporary ciphertext file, finalize that file, transactionally commit its metadata reference, clean staging, then report saved. Database and filesystem updates require restart reconciliation; they are not one atomic transaction. Keep recoverable failures visible and distinguish cleanup pending from complete success.
- Keep a separately managed media key protected by platform storage; authenticate recording/moment identifiers and format version. Missing keys must not trigger silent replacement or data reset. Plan tamper rejection, pending-file recovery, deletion retries and removal of associated audio when a moment is deleted. App unlock, backup/restore and missing-key recovery remain release gates.
- Plaintext exposure during capture and any temporary playback is a known design tradeoff: use private owned paths, minimize lifetime, reconcile leftovers, and never describe capture as already encrypted. Lock/app switch should stop and preserve, with no automatic restart; force-kill may leave an invalid unfinished recording, which must be reported honestly rather than promised recoverable.
- Rikesh chose about **4 minutes per clip** for the first voice increment. Plan a visible timer and automatic stop at four minutes, followed by the normal encrypted-save flow; another clip starts only on an explicit Record action. Several clips per moment remain supported in the plan. Proposed encoding is mono AAC at 64 kbps: about 0.48 MB/minute or 1.92 MB for four minutes before container/encryption overhead. Verify actual quality, size and native duration enforcement on the S20. Permit one recorder/player operation at a time, enforce native byte/duration limits, and reserve free-space headroom for staging plus ciphertext. Bound list loading and account for aggregate saved/draft bytes as well as per-clip size. Aggregate storage policy and headroom thresholds remain to be settled and measured. Never silently delete original recordings to free space.
- Acceptance work still to run: permission denial, immediate Stop/empty audio, repeated taps, lock/app switch, call/audio-focus interruption, cold restart, forced termination at save stages, low space, ciphertext tampering, missing key, deletion cleanup and repeated capture/play resource measurements. No voice behavior or media encryption was verified today.

Rikesh approved the concrete [voice-recording design](superpowers/specs/2026-09-21-voice-recording-design.md), including the private temporary plaintext tradeoff. Tink Android 1.23.0 with AES256_GCM_HKDF_4KB is now proven for the bounded synthetic-file use case. Proposed initial resource defaults are a 4 MiB clip ceiling, 100 MiB phone headroom plus working space, and a 250 MiB usage warning; real recording quality, usage thresholds and lifecycle behavior still need measurement.

Resume: read the approved design, [completed native proof plan](superpowers/plans/2026-09-21-media-vault-proof.md), latest evidence and Lesson 3. Use the installed updated APK and existing Metro server for the learning panel. Reuse tools/caches and `S:` for the next native build. The ignored local Gradle init file described in Lesson 3 avoids Kotlin C:/S: incremental-cache errors for `still-media-vault` and Expo's generated module list; no routine clean build is required.

### Native encrypted-file milestone (2026-09-21)

- Local Expo module, protected keyset, bounded streaming file operations and explicit development-only Prepare/Verify panel implemented. UI controller persists across page navigation; raw native exceptions are sanitized at native and JS boundaries. Native guards reject proof methods unless both module and application are debug builds (source inspected; release execution still untested).
- Verification: 21 native tests, 26 host tests, typecheck, incremental ARM64 APK build (3m57s; 36 executed / 333 up-to-date), SQLCipher packaging, v2 APK signature and no microphone permission. S20 prepare → force-stop → cold launch → verify passed all ten checks. Full evidence and limits are in the dated review.
- The 75,530,777-byte APK replaced the existing debug artifact and updated the app with `install -r`; no extra APK copy or emulator. No microphone dependency added. Metro is left running for learning; Gradle's single-use build daemon exits after its build.
- Known limit: interrupted initial proof preparation may deliberately fail closed on an orphan key/keyset or incomplete fixture. No reset button is shipped. Never clear app data to repair it; any future repair must be scoped only to the synthetic directory/aliases. General clip/SQLite crash recovery is still the next increment.

### Host clip-recovery milestone and GitHub push (2026-09-21)

- Pushed the approved design and native-file proof through `45862b2`; remote feature branch SHA verified. No merge to `main`.
- Subsequent local source commit `ef268d9` adds `createClipJournal`, opt-in schema 1 to 2 migration, clip intents, cleanup-pending states, durable deletion records/tombstones and bounded recovery. Default app openers and the phone database remain unchanged.
- Root verification: all **46 Node tests** passed (20 new recovery cases), typecheck passed, whitespace check passed. Tests use real SQLite and actual disposable generated files, including close/reopen and injected database/file failures. Host file fixtures are deliberately noncryptographic.
- Failed rollback makes the connection unusable until close/reopen. Public operations serialize, snapshot caller inputs and sanitize errors. Future native integration must retire legacy repository objects and enforce one owner across connections/module reloads; this host core cannot revoke a retained old closure.
- No dependencies, native rebuild, device operations or extra APK copies. No new phone disk measurement; 14.92 GiB above belongs to the preceding native milestone.
- Exercise: from `mobile`, run `node --experimental-strip-types --test tests/clipRecovery.test.ts`. Use [Lesson 4](learning/04-recoverable-clip-saving.md) to predict why the interrupted save yields one clip and why a repeated recovery pass does nothing.
- Resume: read the [host recovery review](reviews/2026-09-21-clip-recovery-core.md) and contracts in `mobile/src/storage/clipTypes.ts`, then design/implement the native file adapter and single-opener S20 integration. Do not activate schema 2 through the existing schema-1 opener or describe the host fixtures as encrypted audio.

### Follow-up: phone exercise connected (2026-09-21)

ADB detected the authorized S20, USB forwarding for port 8081 succeeded, and Metro started with IPv4 preference, offline mode and two workers using the installed Expo CLI. The host health endpoint returned `packager-status:running`; opening the installed app at `http://127.0.0.1:8081` returned Android launch status `ok`, and Metro completed the Android bundle (782 modules). No APK rebuild or dependency installation was performed. Metro is intentionally left running for continued learning; repeat Lesson 2's Metro/USB steps if that process has stopped. Rikesh subsequently confirmed saved status, the history entry and the reminder-snapshot comparison. These are user-reported phone interaction checks, not new restart or encryption evidence.

### Native clip/session milestone (2026-09-21)

- Local native commit `31a3698` and session commit `f839ecd` passed their independent task reviews. No further push or merge was performed; GitHub remains at `45862b2`.
- Verified 47 native tests, 67 host tests, typecheck, incremental ARM64 build (2m30s; 12 executed / 357 up-to-date), SQLCipher packaging, v2 APK signature, no microphone permission and `allowBackup=false`.
- Updated the S20 with `install -r`, preserving app data. **Prepare recovery test** displayed **1 saved · 1 pending**. After cold restart, startup and explicit Check displayed **2 saved · 0 pending** with both finals authenticated and temporary files absent. The generated clips each measured **16044 bytes / 1000 ms** through the queued clip facade.
- **Delete test moment**, another cold restart, and explicit **Check recovery** confirmed **0 saved · 0 pending**, both clips absent and deletion recorded. Filename-only inspection found only the protected keyset left in the durable clip directory. No real journal text, keys or audio were read into the exercise diagnostics.
- USB briefly went offline/unauthorized. Restarting the laptop ADB service and reconnecting/authorizing the phone restored access; no app data was cleared. The successful development bundle load took about 20 seconds in the observed reconnect, not a standalone launch benchmark.
- Resume with the same installed APK and running Metro: verify `adb devices -l` says `device`, restore USB forwarding, and cold-launch. The retained marker should still report completed test deletion. For another lesson, explicitly Prepare a new test, predict the restart outcome, then follow [Lesson 5](learning/05-native-clip-recovery.md). Do not clear app data to repeat the lesson. The next code increment is microphone capture/playback; read the approved design and exact SDK 57 Audio APIs first.
- Follow [Lesson 5](learning/05-native-clip-recovery.md) and record actual outcomes in the [dated review](reviews/2026-09-21-native-clip-integration.md). Native metadata is not microphone/playback evidence; release, lifecycle, backup/restore and power-loss gates remain.

### Session wrap-up (2026-09-21)

- **Published:** approved design, native vault, host coordinator, single encrypted session, learning panels/tests, S20 evidence and lessons through `f9e1587`. The current handoff and refreshed graph are included in this wrap-up commit on the same feature branch. No merge, release deployment or APK upload.
- **Fresh wrap-up checks:** `npm.cmd test` passed 67/67, `npm.cmd run typecheck` passed. The earlier 47 native tests, APK checks and S20 exercise remain the recorded device/native evidence; no unnecessary rebuild or device exercise was repeated.
- **Last phone result:** the generated test moment and both clips remain deleted after a cold restart. The small exercise marker, deletion tombstones and protected media keyset remain. Existing journal entries were not cleared. A new explicit Prepare uses fresh IDs.
- **Resources:** Metro/process stopped, port 8081 checked absent, app closed and USB tunnel removed. JDK/SDK/Gradle caches, dependencies and the single current APK remain for reuse. Latest wrap-up disk observation: 15.04 GiB free before service shutdown. No new dependencies, emulator, clean build or APK copies were needed for wrap-up.
- **Privacy:** publish source, tests, approved design, lessons, reviews and graph only. No tracked APKs, private key files, keystores, local tools or private-data files were found in the repository path check. Local diagnostics remain ignored; the exercise screenshot/UI dump had already been removed.
- **Next code increment:** explain recorder lifecycle; create a small implementation plan from the approved design; integrate SDK 57 Expo Audio with the existing saved-moment/clip ownership; add explicit Record / Stop / Play and private staging/playback cleanup. Several clips per moment and four minutes per clip remain approved. Request microphone permission only on Record and stop/preserve on leaving the foreground. Verify actual AAC size/quality, permission/empty-capture cases and lifecycle behavior on S20 before calling recording complete.
- **Keep visible:** app unlock, encrypted backup/export/restore, missing-key recovery UX, aggregate media usage, repeated-cycle resource measurements, release signing/privacy and release permissions/backup checks remain open. The documented minor UTF-16 admission distinction for path-only native deletion is deferred; inspect it when extending the recorder boundary. CI activation still requires separate GitHub workflow-scope authorization.
- **Graphify:** targeted navigation refresh, currently 266 nodes / 376 edges, covering the inspected session/native source and current handoff. Dependencies, generated native output, local tools and private data are excluded; the graph is a navigation aid rather than security evidence.

Verify repository continuity from the project folder:

```powershell
git status --short --branch
git log -5 --oneline
git ls-remote origin refs/heads/codex/android-device-setup
```

Stay on `codex/android-device-setup`; compare `git rev-parse HEAD` with the remote tip. If another checkout needs updating, inspect its changes first and use a fast-forward update. For the phone, follow Lesson 2's existing-tool environment and Metro/USB commands. Confirm ADB says `device`, not `unauthorized`; if USB reconnect loses forwarding, restore `adb -d reverse tcp:8081 tcp:8081`. Read [Lesson 5](learning/05-native-clip-recovery.md), [current evidence](reviews/2026-09-21-native-clip-integration.md), [approved voice design](superpowers/specs/2026-09-21-voice-recording-design.md) and `mobile/AGENTS.md` before the next app change.

## Capability status and remaining roadmap

| Area | Status / next work |
| --- | --- |
| Voice capture | Record/Stop/Play, encrypted clips and recovery implemented with bounded S20 evidence; physical interruption and earlier resource investigations remain |
| Personal support | Editable cards, bounded imported image/audio combinations and separate explicit playback implemented; current source review state is above |
| Sessions and review | Manual grouping, Start/End, recoverable notes, immutable final writing and separate reflections implemented; timeline ownership/retry corrections are verified above |
| Design | System/Light/Dark preference persists across process restart; broader themes, reduced-motion animation and wider usability/accessibility checks remain |
| Data ownership | Tested encrypted backup/export and restore; recovery design for missing keys; complete deletion/media cleanup |
| Security | App unlock, release permissions/backup validation, logs and secret handling, dependency review, lifecycle/failure tests; no absolute security claims |
| Performance | Media caps and bounded history paging implemented and exercised; peak memory, sustained resources, low-space and broader responsiveness checks remain |
| CI | Workflow exists in `docs/ci/checks.yml`; inactive because current GitHub credential lacks workflow scope. Activate later with explicit credential authorization |
| GitHub integration | Feature branch is published; review/merge to the default branch remains. Graph and handoff are project memory, not proof all roadmap items are implemented |
| Release | Standalone signed preview APK, signing/key management, privacy documentation, test distribution, then selected public release channel; no store/cloud deployment yet |
| Portfolio | Add current device screenshots using synthetic data, walkthrough and eventual release link; keep decisions/tests/learning notes current |
| Later versions | A/B/C game protocol, transcription, evidence-linked patterns, preparation routines, accounts/sync, iOS and web review |

## Decisions to retain

Personal use first, Android first, room to grow. The app supports reflection rather than predicting trades or diagnosing emotions. Starter reminders are original and informed by the general ideas in Mark Douglas's and Jared Tendler's books. Tap automatically logs time and opens injecting logic. Support remains available if saving fails. Recording stops and preserves on lock/app switch. Trading sessions use explicit manual grouping and separate immutable reflections; encrypted backup/restore and recovery UX remain open. Success combines noticing earlier, pausing/following one's process and collecting useful reflection material.

Rikesh has some programming experience and wants to learn the core principles throughout. Each increment should explain the problem and concept, provide a runnable result and small exercise, and record concrete verification. Keep private journals, recordings, keys, APKs, local diagnostics and downloaded tools out of GitHub.

## Navigation

- [README](../README.md): capabilities and run commands.
- [MVP scope](../MVP-SCOPE.md): desired product; not all implemented.
- [Security and memory requirements](../SECURITY-AND-PERFORMANCE.md): release gates.
- [Lesson 1](learning/01-first-moment.md), [Lesson 2](learning/02-android-device-build.md): learning and setup.
- [Lesson 3](learning/03-encrypted-files.md), [native-file proof evidence](reviews/2026-09-21-media-vault-proof.md): encrypted fixture, key persistence, limits and next increment.
- [Lesson 4](learning/04-recoverable-clip-saving.md), [host recovery evidence](reviews/2026-09-21-clip-recovery-core.md): coordinator fundamentals and historical host-only checkpoint.
- [Lesson 5](learning/05-native-clip-recovery.md), [native integration evidence](reviews/2026-09-21-native-clip-integration.md), [integration plan](superpowers/plans/2026-09-21-native-clip-integration.md): current native adapter/session and exact S20 recovery checkpoint.
- [Android device evidence](reviews/2026-09-20-android-build-setup.md): build, native tests and limits.
- [Graphify report](../graphify-out/GRAPH_REPORT.md), [interactive graph](../graphify-out/graph.html): source/document relationships. Query the graph to navigate, then verify claims against current source and this status file. Historical reports describe their own checkpoints.

Suggested next-session prompt: **Continue still. from the current checkpoint in docs/PROJECT-STATUS.md and its dated review. Finish any outstanding correction/verification before starting another increment. Preserve the S20 journals and reuse the installed APK, short-path toolchains and caches. Explain the next small step and keep feature completion separate from release readiness.**
